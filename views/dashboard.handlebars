    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="button is-primary is-light is-pulled-right">Create Post</button></a>
        
        <h1 class="title is-2 pb-4 has-text-info"> * Dashboard *</h1>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="button mb-4 mt-4 ml-5 is-warning is-light">Edit Post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">Hi, there! When you make a blog post, you'll see it here.</h6>

{{/if}}


